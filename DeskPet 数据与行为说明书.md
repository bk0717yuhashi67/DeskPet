# DeskPet 数据与行为说明书

> 覆盖：采集了什么 → 八项指标怎么算 → 九种心情怎么判 → 心情如何变成
> 台词与动作 → 什么时候闭嘴 → 界面上每个数字怎么读。
> 逐条对照源码核对（analyzer.py / mood.py / features.py / budget.py /
> sampler.py / clips.py / lines.py），末章列出 README 与代码实际不一致处。

---

## 0. 一页速览

四层流水线：

    采集原始计数  →  算 8 项指标  →  判 9 种心情  →  决定「说什么 + 做什么 + 动作频率」

- 采集层：`monitor/sampler.py`（1 秒查窗口与空闲，500ms 消费键鼠队列，30 秒落库）
- 计算层：`monitor/analyzer.py`（纯函数，可用构造数据断言）
- 心情层：`pet/mood.py`（9 种心情 + 滞回确认）
- 表达层：`care/features.py` 的 `MOOD_VOICE` + `pet/mood.py` 的 `EFFECTS`
- 闸门层：`care/budget.py`（每日 8 条、间隔 8 分钟）+ `pet/state_machine.py`（免打扰）

核心设计取舍：**心情不同 → 表情不同 + 动作不同 + 说的话不同**，三者必须成套，
不许出现「算了半天数据结果一句话不说」。

---

## 1. 采集层：到底采了什么

### 1.1 节流表

| 周期 | 做什么 |
|---|---|
| 1 秒 | 前台窗口句柄、空闲时长（GetLastInputInfo） |
| 500 毫秒 | 消费键鼠钩子队列（单批最多 4000 条） |
| 2 秒 | 全屏 / 会议检测（决定要不要自动静音） |
| 30 秒 | 落库（或前台切换时立刻落库） |

### 1.2 原始量（14 项）

active_s｜idle_s｜locked_s｜keystrokes｜backspaces｜clicks｜mouse_dist_px｜
app_switches｜late_minutes｜idle_gap_cnt｜focus_block_cnt｜longest_focus_s｜first_ts｜last_ts

### 1.3 三个关键判定规则

1. **离开**：空闲 ≥ 60 秒才算离开（`IDLE_THRESHOLD`），并清零"连续活跃"。
2. **看视频 / 玩游戏算"人在"**：前台分类属「视频」「游戏」或全屏时，
   即使没有键鼠输入也不计离开——否则看两小时电影会被记成"离开两小时"，
   视频时长全丢。但这类场景会**打断专注块**，免得一场电影被记成 100 分钟深度专注。
3. **专注块**：同一 exe 连续停留 ≥ 10 分钟（`FOCUS_MIN_S`）才成块，
   换应用或空闲 ≥ 60 秒即结束；块的时长会参与 `longest_focus_s`。

#### 什么是"全屏"（`is_fullscreen`）

这个判定同时服务两件事：**决定企鹅要不要隐藏**，以及上面第 2 条的"人在不在"。
两处用的是同一个函数，所以判错的代价是两个方向都会出问题。

判据（按顺序）：

1. 窗口可见、未最小化，且类名不在外壳白名单（`Progman` / `Shell_TrayWnd` 等），
   也不以 `#3276` 开头（桌面窗口的类名是数字原子 `#32769`，不在白名单里）。
2. **没有标题栏与边框**：样式位里不含 `WS_CAPTION` / `WS_THICKFRAME` / `WS_BORDER`。
3. 尺寸 ≥ 显示器的 99%。

> **不要检查 `WS_MAXIMIZE`**。这一点踩过坑，两个方向都错过：
>
> - 最早只看尺寸（92% 阈值）。最大化窗口占满工作区 → 误判全屏
>   → 症状"窗口一最大化，企鹅就消失"。
> - 修上一条时加了"有 `WS_MAXIMIZE` 就判非全屏"。但 **Chromium 系浏览器视频全屏时
>   仍保留 `WS_MAXIMIZE`** → 真全屏被漏判 → 症状"浏览器看视频全屏，企鹅不隐藏"。
>
> 真机实测的两种形状（同一窗口 hwnd=1640492，Edge 播 bilibili）：
>
> | 状态 | rect | CAP / THICK / BORDER | MAX | 判定 |
> |---|---|---|---|---|
> | 最大化 | `(-7,-7,1543,831)`（比屏幕大，Win10+ 外扩约 7px） | 全 True | True | False |
> | 视频全屏 | `(0,0,1536,864)`（精确铺满） | 全 False | True | **True** |
>
> 两者 `WS_MAXIMIZE` 都是 True，**只能靠"有没有边框"区分**。

### 1.4 隐私边界

- 键盘**只记次数与时间间隔**，不记任何字符内容。
- 窗口标题可单独关闭（关掉后浏览器只能归到「浏览器-未分类」）。
- 全部数据只进本地 `data/` 目录，零联网。
- 若检测到"前台一直变但两分钟零按键"，判定钩子降级（管理员权限程序内的输入收不到），
  面板会提示"数字可能偏小"，并让企鹅说一句 `hook_degraded`。

---

## 2. 计算层：八项指标的公式与分档

### 2.1 基础量

    退格率 br   = backspaces / max(1, keystrokes)，但 keystrokes < 20 时记 0（小样本噪声保护）
    切换率 sr   = app_switches / max(active_s / 3600, 1.0)       单位：次/小时（至少按 1 小时计，避免短时样本爆量）
    输入密度 ipm = (keystrokes + 1.5 × clicks + mouse_dist_px / 60) / 活跃分钟
    wpm        = (keystrokes / 5) / 活跃分钟
    连打中位数 burst_med = 把按键时间戳按"间隔 ≤ 1.5 秒算同段"切段后的长度中位数

其中连打切段规则：只有时间戳总数 ≥ 3 才计算，间隔 > 1.5 秒即断段，
返回段长中位数（对偶发长按不敏感）。

### 2.2 六项核心指标

**专注度 focus（0-100）**

    focus = 100 × ( 0.50 × clamp(最长专注块 / 2700)
                  + 0.30 × clamp(1 - sr / 60)
                  + 0.20 × f_back )

    f_back = keystrokes < 50 时取 0.5（中性）；否则 clamp(1 - (br - 0.08) / 0.22)

满分参照：最长专注块 45 分钟、切换率 0、退格率 ≤ 8%。
注意：旧公式在「没敲键盘」(br=0) 时反把 f_back 算成满分（误判"退格极少=极度专注"），
现已加 keystrokes<50 退化保护，未输入时给中性 0.5。

**活跃度 activity（0-100）**

    presence    = clamp(active_s / 7200)                 在场 2 小时 = 满分基础分量
    input_part  = clamp( (ipm - 10) / (180 - 10) )      输入密度：10 ipm 起算，170 ipm 满
    activity    = 100 × ( 0.35 × presence + 0.65 × input_part )

旧公式 `activity = 100 × clamp((ipm - 15)/205)` 完全依赖输入密度：
一旦键盘/鼠标钩子失效、或纯鼠标/阅读场景，输入为 0 → 活跃度**恒为 0**（名不副实）。
现改为"在场基础分量 + 输入密度分量"，即使钩子失效，只要人在电脑前就有合理活跃度。

**疲劳度 fatigue（0-100，带跨日平滑）**

    raw = 0.30 × clamp(活跃时长 / 10h)
        + 0.25 × clamp(连续活跃 / 3.5h)
        + 0.25 × clamp(深夜分钟 / 180)
        + 0.20 × clamp((max(0, 空闲碎片化 - 2)) / 8)
    today  = 100 × clamp(raw)
    fatigue = 0.65 × today + 0.35 × 昨日 fatigue      （取不到历史时直接用 today）

空闲碎片化 = 空闲次数 / max(活跃小时, 0.5)，用来区分"深度工作"和"每两分钟被打断一次"。

**熬夜指数 stayup（0-100，滚动三天加权）**

    today = 100 × clamp(深夜秒数 / 25200)           深夜=23:00–05:00（6 小时=21600 秒窗口）；
                                                  sampler 按秒累计 late_minutes，封顶须用秒级（7 小时=25200）
    若今天最后活跃时刻在 0-5 点 → today = min(100, today + 10)    附加惩罚 15→10
    若昨天 stayup ≥ 60 且今天首次活跃早于 7 点 → today = max(0, today - 10)
    stayup = 0.5 × today + 0.3 × 昨天 + 0.2 × 前天

**心流强度 flow（0-1）**

    flow = clamp( 0.5 × clamp((连打中位数 - 8) / 40)
                + 0.3 × clamp(wpm / 60)
                + 0.2 × (1 - clamp(br / 0.30)) )

前半段权重最大：连续不间断地敲，比敲得快更能说明"进状态了"。

**分心指数 distraction（0-100）**

    distraction = 100 × clamp( 0.6 × clamp(sr / 60) + 0.4 × (1 - clamp(最长专注块 / 2700)) )

（sr 满量程从 40 放宽到 60，与切换率分档阈值对齐，避免普通多任务用户被误判"严重碎片化"。）

### 2.3 分档文案（面板直接显示这些词）

| 指标 | 分档 |
|---|---|
| 专注度 | ≥75 深度专注 ｜ ≥50 比较稳 ｜ ≥25 有点散 ｜ <25 很碎片 |
| 疲劳度 | ≥75 很累 ｜ ≥55 偏累 ｜ ≥30 还行 ｜ <30 精神 |
| 活跃度 | ≥60 高强度 ｜ ≥20 正常 ｜ <20 偏静 |
| 熬夜指数 | ≥75 严重熬夜 ｜ ≥50 连续晚睡 ｜ ≥20 偶有晚睡 ｜ <20 作息正常 |
| 切换率 | >60 严重碎片化 ｜ >35 偏分心 ｜ >12 正常 ｜ ≤12 深度专注 |
| 退格率 | >25% 反复改 ｜ >15% 偏多 ｜ >6% 正常 ｜ ≤6% 很流畅 |

### 2.4 一个完整算例（把数字走到底）

某天：活跃 3h20m（12000 秒）、12000 按键、2400 退格、800 点击、
鼠标移动 120 万像素、90 次切换、连续活跃 2h、20 次空闲碎片、最长专注 50 分钟。

    br = 2400/12000 = 0.20（20%）
    sr = 90 / 3.333 = 27.0 次/小时
    focus = 100 × (0.5×1.0 + 0.3×(1-27/40) + 0.2×(1-(0.20-0.08)/0.22))
          = 100 × (0.500 + 0.0975 + 0.0909) = 68.8   → 比较稳
    ipm  = (12000 + 1200 + 20000) / 200 = 166
    activity = 100 × (166-15)/205 = 73.7              → 高强度
    fatigue = 100 × (0.30×0.333 + 0.25×0.571 + 0 + 0.20×0.5) = 34.3 → 还行
    wpm  = 2400/200 = 12
    flow = 0.5×0.425 + 0.3×0.2 + 0.2×0.333 = 0.339
    distraction = 100 × (0.6×0.675 + 0) = 40.5
    stayup = 0

结论这条数据会把企鹅推到「**干劲十足**」（见下一章优先级推演），
配 celebrate 动作、说"今天敲得真带劲！"、随机动作频率 ×1.4。

---

## 3. 心情层：九种心情的判定

### 3.1 判定顺序即优先级（命中即返回，不继续往下判）

| # | 心情 | 触发条件（源码原文） |
|---|---|---|
| 1 | 熬夜关切 CONCERN_STAYUP | `stayup > 70` **或** 当前小时 ∈ [1, 5) |
| 2 | 疲劳关切 WORRIED_TIRED | `fatigue > 75` |
| 3 | 专注陪伴 FOCUS_COMPANION | `focus > 70` 且 `flow > 0.6` 且 `activity > 40` |
| 4 | 有点卡住 FRUSTRATED | `br > 0.25` 且 `activity > 40` 且 `keystrokes > 300` |
| 5 | 有点分心 DISTRACTED | `distraction > 65` 且 `active_s > 1800` |
| 6 | 状态不错 FOCUSED | `focus > 75` 且 `active_s > 1200` |
| 7 | 干劲十足 CHEERFUL | `activity > 60` 且 `fatigue < 45` |
| 8 | 有点困 SLEEPY | `idle_now ≥ 300` 或（`60 < active_s < 300` 且 `keystrokes < 30`） |
| 9 | 平静 CALM | 兜底 |

把 2.4 的算例代进去：第 1 条 stayup=0 且时间不是凌晨，不中；第 2 条 34.3 ≤ 75，不中；
第 3 条 68.8 ≤ 70，不中；第 4 条 20% ≤ 25%，不中；第 5 条 40.5 ≤ 65，不中；
第 6 条 68.8 ≤ 75，不中；第 7 条 73.7 > 60 且 34.3 < 45 → **干劲十足**。

### 3.2 优先级设计意图

- 熬夜和疲劳排最前：这两件是"最需要被关心"的事，其他状态都得让位。
- 专注陪伴排第三：一旦进心流就闭嘴，这是整个项目最核心的一条设计。
- 「状态不错」（第 6）比「干劲十足」（第 7）更优先：专注比热闹更值得肯定。
- 第 8 条兜住了"刚开机还没开始敲"和"人在屏幕前但不敲了"两种情况。

### 3.3 滞回（防临界反复横跳）

心情切换需要**连续 2 次确认**才生效。心情每 30 秒重算一次（`TICK_MS = 30_000`），
所以正常切换延迟约 **1 分钟**。
**例外**：熬夜关切与疲劳关切被标记为 urgent，**立即生效、不需要确认**。

---

## 4. 表达层：心情 → 台词 + 动作

### 4.1 `MOOD_VOICE`：每种心情的专属台词场景 + 专属动作

| 心情 | 台词场景 | 专属动作 | 实际文案例子 |
|---|---|---|---|
| 疲劳关切 | `care_tired` | **yawn（打哈欠）** | "你连着好几个小时了，站起来走两步吧。" |
| 有点卡住 | `frustrated` | **sad（难过）** | "改了好多遍呀，要不要先喝口水再想？" |
| 有点分心 | `distracted` | **look_around（东张西望）** | "今天开了好多窗口呢，要不要挑一件先做完？" |
| 状态不错 | `focused` | **nod（点头）** | "状态不错，我陪着你。" |
| 干劲十足 | `cheerful` | **celebrate（庆祝）** | "今天敲得真带劲！" |
| 有点困 | `sleepy` | **blink_double（连眨两下）** | "有点安静呢，你还在吗？" |

**没有** MOOD_VOICE 条目的四种心情（这点很重要）：

- 熬夜关切 → 不走本表，它的"sad"动作与 `care_stayup` 台词来自 `_check_stayup`
  这条**独立路径**（条件 `stayup ≥ 50`，**或当前为凌晨 1-5 点**——后者解决了
  "深夜被判定熬夜关切却一句话不说"的历史 bug），见第 5 章。
- 专注陪伴 → 由 `_check_flow` 单独处理，只在进入/离开两个瞬间开口（第 5 章）。
- 平静 / 睡着 → 不主动开口。

### 4.2 `EFFECTS`：每种心情对动作频率、播放速率、表情的改动

| 心情 | 动作频率倍率 | 动画速率 | 允许主动气泡 | 表情叠加 |
|---|---|---|---|---|
| 熬夜关切 | ×0.4 | ×0.9 | 是 | 眯眼 0.4 |
| 疲劳关切 | ×0.35 | **×0.8（整体放慢）** | 是 | — |
| 专注陪伴 | **×0（完全不动）** | ×0.95 | **否** | — |
| 有点卡住 | ×0.6 | ×1.0 | 是 | 眯眼 0.35 |
| 有点分心 | ×0.8 | ×1.0 | 是 | — |
| 状态不错 | ×0.5 | ×1.0 | 是 | — |
| 干劲十足 | **×1.4** | ×1.05 | 是 | 脸红 0.25 |
| 有点困 | ×0.3 | ×0.85 | 是 | 眼睛半闭 0.7 |
| 平静 | ×1.0 | ×1.0 | 是 | — |
| 睡着 | ×0 | ×1.0 | 否 | — |

动作频率倍率直接乘到设置里的"随机小动作上限"（默认 3 次/小时）上：
所以「干劲十足」时每小时最多约 4 次，「疲劳关切」时约 1 次，「专注陪伴」时归零。

### 4.3 动作频率的两道额外限制（`pet/actions.py`）

- 两次随机动作最小间隔 45 秒、最大 120 秒；
- 每小时预算被**均匀铺开**（下限 = 3600 / 每小时次数 × 0.85），
  避免"前三下扎堆出现、然后安静一小时"。
- 动作从 `RANDOM_LIGHT` 这 8 个里抽：
  blink_double / look_around / wing_flap / hop / stretch / yawn / waddle / nod。
  另外 14 个动作（celebrate、sad、eat、throw_ball…）**永不被随机触发**。

### 4.4 心情对企鹅的完整影响面

改三件事（源码 `pet/mood.py` 顶部注释）：
1. 待机姿势微调（表情、动作快慢）；
2. 是否主动说话、说什么；
3. 随机动作频率（心流中直接归零）。

---

## 5. 不由心情驱动的主动行为（全部阈值）

| 行为 | 触发条件 | 台词场景 | 动作 |
|---|---|---|---|
| 初次见面 | 启动后约 6 秒，**仅第一次启动**触发（已落地 `met_before` 标记，之后每次重启不再弹） | `first_meet` | — |
| 早间问候 | 活跃 ≥ 60 秒 且有前台窗口，且当前小时 < 11，每天一次 | `morning` | — |
| 晚安 | 进入睡眠时段（默认 23:30–07:00）时，每天一次 | `night` | — |
| 离开回来 | 空闲 ≥ 15 分钟后再活跃（空闲 < 5 秒），且离开时长 ≥ 15 分钟 | `welcome_back` | — |
| 补报漏提醒 | 回来时若有超宽限期被跳过的提醒 | `missed` | — |
| 起身 | 连续活跃 ≥ 50 分钟 且 今日活跃 ≥ 5 分钟 | `care_sit_long` | **stretch** |
| 护眼 | 距上次 ≥ 45 分钟 且 今日活跃 ≥ 45 分钟 | `care_eyes` | **look_around** |
| 喝水 | 距上次 ≥ 75 分钟 且 今日活跃 ≥ 75 分钟 | `care_water` | **nod** |
| 熬夜关切 | `stayup ≥ 50`，每天一次；连续 ≥ 3 天用加强版；`stayup > 70` 升级为紧急 | `care_stayup` / `care_stayup_hard` | **sad** |
| 饭点漏吃 | 12:40–14:00 或 18:40–20:00，且今日活跃 ≥ 1 小时、连续活跃 ≥ 40 分钟，每天一次 | `care_meal_skip` | **eat** |
| 里程碑 | 安装天数 ∈ {7, 30, 100, 365}，每天判一次 | `milestone` | **celebrate** |
| 无聊自娱 | 距上次互动 ≥ 30 分钟 | `self_play` | **wing_flap** |
| 自言自语 | 空闲 ≥ 240 秒（人还在，只是没敲键盘） | `nobody_here` | — |
| 心流进入 | 心情刚变成「专注陪伴」 | `focus_start` | **不说话** |
| 心流结束 | 离开专注陪伴且持续 ≥ 10 分钟 | `focus_end` | **不说话** |
| 忙完补话 | CPU 占用 ≥ 60% 持续 ≥ 120 秒后回落 | `busy_no_disturb` | — |
| 番茄钟开始 | 右键启动，默认 25 分钟 | `pomodoro_start` | **scarf_put_on** |
| 番茄钟结束 | 倒计时归零（紧急 + 粘性气泡） | `pomodoro_end` | **celebrate** |
| 番茄钟中止 | 手动停止 | `pomodoro_stop` | — |
| 被戳吐槽 | 连点累计 ≥ 3 次且预算可用 | `click_fun` | 轮换 5 种被戳动作 |
| 当日小结 | 22:00 的「休息」提醒触发时，附加在提醒文案后面 | `summary` / `summary_zero` | 该提醒自带动作 |
| 钩子降级 | 检测到降级且今日活跃 > 15 分钟，只提醒一次 | `hook_degraded` | — |
| 提醒到点 | 12:00 / 18:00 / 22:00 三条预设 + 自建提醒 | `remind_*` | 按提醒的 action 映射 |
| 漏提醒补播 | 关机/休眠错过的提醒，宽限期内（饭点 90 分钟、休息 60 分钟、自定义 30 分钟）补一次 | `remind_eat_late` 或"刚才 HH:MM 我提醒过一次：…" | 同上 |

补充说明：

- **心流中刻意完全安静**是核心设计，所以只在"刚进入"与"长段结束"两个瞬间开口。
- **忙的时候企鹅本来就闭嘴**，所以那句"刚才你忙得正紧，我没吵你"只能等忙完再补，
  顺带解释了"刚才为什么没理你"。
- 番茄钟的安静理由是**独立的一条**（`pomodoro`），结束番茄钟只清它自己，
  不会误清掉全屏/会议/心流这些由环境推导出来的理由。

---

## 6. 闸门层：能不能开口

### 6.1 打扰预算（`care/budget.py`）

- 每日上限：**8 条**主动消息（设置可改）；
- 两条之间最小间隔：**8 分钟**（设置可改）；
- **紧急消息**（熬夜 > 70、番茄钟结束、休息提醒）：**不受每日上限约束**，
  但仍保留 **60 秒**防抖；
- 跨零点自动重置计数。
- 随机小动作另有独立预算：**每小时 3 次**（受心情倍率缩放）。

### 6.2 免打扰理由（可叠加，任一存在就闭嘴）

| 理由 | 判据 |
|---|---|
| `fullscreen` | 前台窗口全屏（视频 / 游戏 / 演示） |
| `meeting` | 会议进程，或标题命中会议关键词 |
| `focus` | `focus > 70` 且 `flow > 0.6` 且 `active_s > 1200`（企鹅主动安静） |
| `busy` | **整机 CPU 占用 ≥ 60%**（编译/渲染/打包） |
| `night` | 夜间睡眠时段（可在设置里关） |
| `pomodoro` | 番茄钟进行中 |

> `busy` 用整机 CPU 而不是进程名白名单——按进程名会把长期挂着的
> node / git 误判成"在忙"，导致企鹅永久闭嘴。

### 6.3 状态层面的禁止（`pet/state_machine.py`）

禁止主动行为的四个状态：拖动 DRAG、游戏 GAME、提醒播报 REMIND、睡眠 SLEEP。
优先级（谁能打断谁）：拖动 100 > 游戏 80 > 提醒 60 > 动作 40 > 睡眠 30 > 待机 20。

一个细节：**游戏中不算被打扰**（用户正在玩），所以 `is_quiet` 在 GAME 下返回 False。

### 6.4 两层闸门的关系

`is_proactive_blocked`（状态 + 免打扰）**先判**，`budget.can_speak()` **后判**。
也就是：先问"现在该不该说话"，再问"今天还说得出话吗"。

---

## 7. 界面阅读指南（数据面板）

| 区块 | 内容 |
|---|---|
| 顶部标题 | 「今日状态 · 日期」+ 一行**「企鹅看来你：干劲十足」**——即当前心情的中文名 |
| 指标卡 ×5 | 今日活跃（时长）、专注度、疲劳度、活跃度、熬夜指数。每张卡 = 数值 + 分档词 + 进度条 |
| 今日小结 | 一行摘要：活跃时长 · 最长专注段（及段数）· 最常用的应用；休闲超 10 分钟会追加 |
| 应用时长排行 | 横向条形图 |
| 时间都去哪儿了 | 环形图 + 图例（按「工作/学习/社交/会议/视频/游戏/浏览器/工具/系统/其他」十类分色） |
| 今天的作息分布 | 24 小时热力条（按会话明细摊到每小时，相对峰值归一化）——比总时长更能说明作息问题 |
| 最近 7 天活跃时长 | 迷你折线 |
| 输入画像 | 按键次数（含退格数）、退格率+分档、鼠标点击、鼠标移动（千像素）、应用切换+分档、专注块（段数 + 最长）、首次/最后活跃时刻、打字节奏 |
| 还能统计什么 | 尚未实现的候补清单：空闲碎片化分布、全屏时长、音频占用、按键类型粗分类、每日首次输入时间推断起床作息、7 日打字速度趋势、专注中被中断次数 |

「打字节奏」这一行显示的是 `flow × 100`，并附判语：
**> 70 显示"心流中"**，> 40 显示"正常"，否则"偏慢"。

---

## 8. 已知不一致与可优化点（核对代码时发现）

按严重程度排序。前四条是**文档与实现不符**，第五条是**代码小 bug**。

1. **README 写"心情切换有 5 分钟滞回确认"，实际约 1 分钟。**
   `care/features.py` 的 `TICK_MS = 30_000`，`MoodTracker` 需要连续 2 次确认，
   即 30 秒 × 2 ≈ 60 秒。若想做满 5 分钟，应把 `confirmations` 提到 10，
   或给 `MoodTracker` 传入更长的判定间隔。

2. **README 写专注陪伴条件是「专注度 >70 且心流 >0.6」，代码还要求 `activity > 40`。**
   见 `pet/mood.py:80`。影响：阅读型的深度专注（不敲键盘）不会被判为心流陪伴。

3. **README 写「有点卡住」条件是「退格率 >25% 且高强度输入」，
   代码还要求 `keystrokes > 300`。**
   见 `pet/mood.py:84`。影响：一天按键不足 300 时不会触发卡住提醒。

4. **（2026-09-21 已修复）README 把熬夜关切的专属动作标为"难过"，
   但 `MOOD_VOICE` 里没有 CONCERN_STAYUP 条目。**
   它的 `sad` 动作实际来自 `care/features.py` 的 `_check_stayup`，
   原路径门槛是 `stayup ≥ 50`。**副作用**：凌晨 1-5 点被判定为熬夜关切、
   但 `stayup` 还没到 50 时，企鹅只会动作变慢、眯眼，**不会主动开口**。
   修复（未采用补 MOOD_VOICE 条目的方案，避免与 `_check_stayup` 每日提醒重复开口）：
   把 `_check_stayup` 的提前返回条件放宽到"stayup ≥ 50 **或** 当前为凌晨 1-5 点"，
   使深夜被判定熬夜关切时也走"care_stayup + sad"这条独立路径正常开口。

5. **「今日活跃」卡片的副标题用错了分档函数。**
   `ui/dialogs/stats_panel.py:272` 里
   `self.tiles["active"].set_data(fmt_duration(m.active_s), analyzer.activity_band(m.activity), ...)`
   ——时长配的是**活跃度**的判语，于是会显示成"今日活跃 3h20m / 高强度"。
   建议给时长单独写一个 `active_duration_band()`。

另有两点不算错误、但值得知道：

6. 心流阈值有两套：心情判定用 `flow > 0.6`，面板"打字节奏"判语用 `flow > 0.7`。
7. **分心指数与心流强度不在顶部指标卡里**。分心指数只进数据库缓存
   （`storage.set_metrics`），面板上不显示；心流以"打字节奏"的名义出现。
   它们都是会真实驱动企鹅行为（分心→东张西望）的指标，但用户看不到原始值。

8. **（2026-09-21 实测发现，已彻底修复）双重闸门导致企鹅几乎全天静默。**
   `CareManager` 各检查点先 `can_speak()` 通过、再 `record()` 扣费，然后 emit 信号给
   `main.show_bubble()`，后者**又检查一次** `can_speak()`——刚扣完费 `last_at=now`，
   5 分钟间隔必然不满足，于是气泡被丢弃但额度已经扣掉。每条关怀消息都"扣费不显示"，
   隐形扣满每日上限后整天闭嘴，只有走 urgent 通道的到点提醒能说出来。
   日志表现：`pet.log` 里从第一天起就有大量"打扰预算已用尽，跳过气泡"，
   连"番茄钟开始""设置已保存""连击 x5"这些互动台词也被误拦。

   修复分两步（缺一不可）：
   (a) `show_bubble` 增加 `skip_budget` 通道，已在其调用侧结算预算的关怀消息、
       游戏反馈、用户操作回应（设置保存/清空数据/提醒确认等）直接显示，不再二次扣费；
   (b) **关键遗漏**：上一版只加了 `skip_budget` 形参，却**没改 `main.py:122` 的
       `care.speak` 连接**——所有心情 / 健康发言都走这条信号，连接里仍按
       `urgent=False` 转发，于是又掉进 `show_bubble` 的二次 `can_speak` 检查被丢弃。
       本次把连接改为 `show_bubble(t, urgent=urgent, skip_budget=True)`，
       才是让企鹅真正开口的那一下。

9. **（2026-09-21 实测发现，已修复公式 + 增加可观测日志）键盘/鼠标钩子从第一天起从未收到任何事件。**
   `daily_stat` 全历史 `keystrokes / clicks / mouse_dist_px` 均为 0，但 `active_s`（系统空闲检测）
   和 `app_switches`（轮询前台窗口）一直有值——说明 pynput 钩子对象虽创建成功（日志报"键盘=True"），
   却从未真正捕获输入事件。这导致**活跃度恒为 0、专注度退格项退化、心流/分心失真**。
   - 旧活跃度公式纯依赖输入密度，输入为 0 必为 0（这是"活跃度为什么是 0"的真正根因，不是算错）。
   - 已改：活跃度 = 0.35×在场分量 + 0.65×输入密度分量，钩子失效时也能反映"在场"；
     专注度退格项加 `keystrokes<50` 中性保护；切换率分母下限 1 小时；分心/切换阈值放宽。
   - 已加可观测日志：`monitor/hooks.py` 暴露 `first_event_ts`；`sampler._drain_events` 在
     **首次收到事件时记"输入钩子已收到事件，键盘/鼠标统计正常"**，启动 60 秒仍零事件则记
     **"启动 60 秒未收到任何键鼠事件，输入钩子可能未生效"** 告警。重启后看 `data/logs/pet.log`
     即可确认钩子到底有没有活——若只看到告警没有"正常"日志，就是钩子本身环境问题
     （权限 / 安全软件 / RDP 会话），需以管理员身份运行或在 PyCharm 里调试。

10. **（2026-09-21 已修复）全屏判定把最大化窗口当成了全屏，导致企鹅被误隐藏。**
   需求是"只有全屏时隐藏，窗口化（含最大化）不隐藏"，但 `is_fullscreen` 原来
   只用尺寸判据（≥ 显示器 92%）。本机 1920×1080 下，最大化窗口**正好占满 100% 工作区**
   （含标题栏），于是被误判为全屏 → 企鹅消失。
   修复：加入样式位判断，凡带 `WS_CAPTION` / `WS_THICKFRAME` / `WS_BORDER`
   的窗口不算全屏，尺寸阈值同时收紧到 99%。
   实测验证：最大化窗口 ratio 0.776×0.907 → `False`；外壳窗口（`Shell_TrayWnd`）→ `False`。

   注意这个函数的**第二个调用点**：`sampler._passive_consuming()` 用它判断
   "看视频/玩游戏时人还在不在"。收紧后影响是：最大化窗口不再被当作"被动消费"，
   因此最大化状态下的长时间无输入会正常计入"离开"。这符合语义——最大化的是编辑器或
   浏览器，本就不该算被动消费；真正的视频/游戏仍由应用分类（`LEISURE_CATEGORIES`）兜住。

11. **（2026-09-21 已修复）第 10 条的修复引入反向 bug：浏览器全屏时企鹅不隐藏。**
   修第 10 条时顺手加了"`style & WS_MAXIMIZE` 就返回 False"，把最大化窗口排除掉。
   但 **Chromium 系（Edge/Chrome）进入视频全屏后仍保留 `WS_MAXIMIZE` 位**，
   只是把标题栏和边框去掉了 —— 于是真全屏被这条判断否决，企鹅不再隐藏。
   用户报告"浏览器视频全屏时企鹅没有隐藏"，实测复现并定位。

   真机抓到的两个形状（同一窗口 `hwnd=1640492`，Edge 播 bilibili）：

   | 状态 | rect | MAX | CAP | THICK | BORDER | 旧判定 | 新判定 |
   |---|---|---|---|---|---|---|---|
   | 最大化 | `(-7,-7,1543,831)` | T | T | T | T | False | False |
   | 视频全屏 | `(0,0,1536,864)` | **T** | F | F | F | **False（漏判）** | **True** |

   修复：**移除 `WS_MAXIMIZE` 判断**，只保留"没有标题栏/边框 + 尺寸 ≥99%"。
   区分两种状态靠的是边框而不是 `WS_MAXIMIZE`。
   顺带修掉同一轮发现的另一个隐患：桌面窗口类名是数字原子 `#32769`（不在
   `SHELL_CLASSES` 里），它铺满整屏且无边框，会被判成全屏 —— 现已用 `#3276`
   前缀挡掉。另把 `GetWindowLongW` 的 `restype` 从 `c_long` 改为 `wintypes.DWORD`，
   避免高位为 1 时读出负数（桌面窗口原先返回 `-1778384896`）。

## 8.1 回归测试（`tests/`）

| 文件 | 覆盖 |
|---|---|
| `test_analyzer.py` | 五维指标公式、分档函数、提醒调度、计分数学 |
| `test_physics.py` | 抛球小游戏的物理 |
| `test_care_triggers.py` | **关怀触发全链路**：见下 |
| `test_fullscreen.py` | **全屏判定**：见下 |

`test_fullscreen.py` 为第 10/11 条 bug 写的回归测试。窗口形状取自真机实测，
用 `_fake()` 临时替换 `winapi` 的取数函数来构造场景（不依赖真实窗口存在）。
钉死的两个方向：

- **必须判为全屏**：Chromium 视频全屏（`MAX=True` 但无边框，1536×864）、
  F11 全屏、1535×864 的 1 像素容差情形。
- **必须判为非全屏**：最大化窗口（`(-7,-7,1543,831)`，带边框）、
  尺寸正好等于显示器的带边框窗口、半屏窗口、桌面原子类 `#32769`、
  `Progman` / `WorkerW` / `Shell_TrayWnd`、不可见/最小化、矩形取不到。
- 另含样式常量值校验与 `GetWindowLongW` 无符号性校验。

`test_care_triggers.py` 是为上述第 8/9 条 bug 写的回归测试，用替身
（`FakeStats` / `FakeSampler` / `FakeStorage` / `FakeStateMachine` / `FakeMain`）
驱动**真实的** `CareManager`，不启动 Qt 窗口、不碰真实数据库。它固化了这些行为：

- **双重闸门**：证明走 `skip_budget=True` 的气泡能显示，且不传时确实会被拦（反证修复必要性）。
- **凌晨熬夜关切**：`stayup<50` 但当前处于凌晨 1-5 点时必须开口，且标记为紧急。
- **心情映射**：`MOOD_VOICE` 里每个心情都有存在的台词场景和非空动作；重复间隔、离席、安静态三道闸门都要生效。
- **健康包**：久坐/护眼/喝水各自的动作，以及"活跃不足 5 分钟"、"处于空闲"时不打扰。
- **心流**：进入/离开各说一句，**中间必须完全安静**（这是核心设计）。
- **安静闸门**：全屏 / 会议 / 心流 都要让 `is_proactive_blocked` 为真，且健康提醒要受其约束。
- **指标管线**：输入钩子全 0 时活跃度仍大于 0；`stayup` 按秒级（25200 秒）封顶。

运行（共 93 个用例）：

```
.venv\Scripts\python.exe -m unittest discover -s tests -v
```

---

## 附录 A：台词库（45 个场景）

| 分组 | 场景 |
|---|---|
| 问候 | morning, welcome_back, night, first_meet |
| 提醒 | remind_eat, remind_eat_late, remind_rest, remind_generic, remind_snooze, remind_ack, missed |
| 关怀 | care_sit_long, care_water, care_eyes, care_stayup, care_stayup_hard, care_tired, care_meal_skip |
| 状态呼应 | focus_start, focus_end, frustrated, distracted, cheerful, focused, sleepy, busy_no_disturb |
| 数据小结 | summary, summary_zero, hook_degraded |
| 小游戏 | game_start, game_catch, game_miss, game_combo, game_end_good, game_end_bad, game_new_record, game_cannot_drag |
| 成就陪伴 | streak, milestone |
| 闲趣 | self_play, nobody_here, click_fun, pomodoro_start, pomodoro_end, pomodoro_stop |

每场景 3~5 条变体，用 `md5(场景|盐|时间戳整除30)` 做确定性轮换，避免连续重复。
设计原则：不用 emoji 当主要表达（部分字体渲染成方框），语气是"陪伴"而非"管教"。
兜底场景为 `click_fun`。

## 附录 B：全部 22 个动作

| 分组 | 动作 |
|---|---|
| 眨眼 | blink, blink_double |
| 待机轻动作（可随机） | look_around, wing_flap, hop, spin, stretch, yawn, waddle, peek, nod |
| 被动互动 | pat_belly, struggle, throw_ball, catch_ball, dizzy, sad, celebrate |
| 睡眠/吃饭/仪式 | sleep_loop, eat, scarf_put_on, wave |

每个动作是一串关键帧（归一化时间 t ∈ [0,1]），由 animator 在相邻帧之间按缓动插值；
未写出的姿势字段从默认姿势继承。
